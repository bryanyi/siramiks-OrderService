spring:
  datasource:
    url: jdbc:h2:mem:order
    username: byi
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  application:
    name: ORDER-SERVICE

eureka:
  client:
    enabled: false